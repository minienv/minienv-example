env: dev
disabled: false
expiration: 300
metadata:
  env:
    platform: "node-alpine"
    vars:
    - name: var1
      defaultValue: "val"
  editor:
    hide: false
    srcDir: ""
  proxy:
    ports:
    - port: 3000
      hide: false
      tabs:
      - name: "Example"
        path: "/"
      - name: "Example 2"
        path: "/index.html"
