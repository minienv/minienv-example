{
  "name": "minienv-example",
  "version": "0.0.1",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cfenv": "^1.0.4",
    "express": "^4.13.3"
  }
}
